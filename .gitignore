# Dependencies
node_modules/
dist/
out-tsc/
.angular
upcoming/

# Environment files (keep .example)
.env
.env.local

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Testing
coverage/
.nyc_output/

# Build
angular.json.bak

# Development
npm-debug.log*
yarn-error.log*

# Runtime logs. A committed application log previously leaked the admin
# credential into this repository's history. Log files are never source.
# Note this ignores log files rather than the logs/ directory itself, so
# logs/GEMINI.md stays trackable.
*.log
logs/*.log
